{
  "name": "misterytrips-site",
  "version": "1.0.0",
  "description": "MisteryTrips marketing site + quiz questionnaire with Brevo (SMTP) transactional email",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "sharp": "^0.35.2"
  }
}
